#!/usr/bin/env python3
"""Install thermalDiffusion to the MoDeNa models directory."""

import subprocess
import sys
from pathlib import Path

HERE = Path(__file__).resolve().parent

subprocess.run(
    [sys.executable, '-m', 'modena', 'install', str(HERE)],
    check=True,
)
