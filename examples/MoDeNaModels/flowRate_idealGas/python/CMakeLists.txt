# Python packaging is handled by the top-level pyproject.toml (scikit-build-core).
#
# To install the package:
#   modena install examples/MoDeNaModels/flowRate_idealGas/
#   # or: pip install examples/MoDeNaModels/flowRate_idealGas/
#
# Python files and the compiled binary are installed by the top-level CMakeLists.txt
# and src/CMakeLists.txt respectively.
