# Python packaging is handled by the top-level pyproject.toml (scikit-build-core).
#
# To install the package (triggers cmake build + install in one step):
#   modena install examples/MoDeNaModels/twoTankR/
#   # or: pip install examples/MoDeNaModels/twoTankR/
