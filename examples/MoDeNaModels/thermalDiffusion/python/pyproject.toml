[build-system]
requires      = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name    = "thermalDiffusion"
version = "0.1.0"
requires-python = ">=3.9"
dependencies    = ["modena", "fireworks"]

[tool.setuptools.package-data]
thermalDiffusion = ["config.toml"]
